name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-python@v4
      with:
        python-version: "3.9"
    
    - name: Test
      run: |
        python --version
        echo "✅ CI is working!"
        
    - name: Success
      run: echo "🎉 All tests passed!"